# =============================================================================
# Supabase — formulário da LP (submitLead → tabela `leads`)
# Vercel: Settings → Environment Variables (prefixo VITE_ obrigatório no client)
# Local: copie para app/.env e preencha
# =============================================================================

VITE_SUPABASE_URL=https://uwnrktjwtflkjspqxfyp.supabase.co

# Publishable / anon (sb_publishable_... ou eyJ...) — NUNCA sb_secret_ aqui
VITE_SUPABASE_ANON_KEY=

# Link do convite do grupo (após envio bem-sucedido do formulário)
VITE_WHATSAPP_GROUP_URL=https://chat.whatsapp.com/LxJOptAAoHM7FzwrCGxNx4?mode=gi_t

# -----------------------------------------------------------------------------
# NÃO use service_role / sb_secret_ em variável VITE_* (bloqueado no browser).
# -----------------------------------------------------------------------------
